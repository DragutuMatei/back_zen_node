{
  "version": 2,
  "builds": [{ "src": "server.js", "use": "@vercel/node" }],
  "routes": [{ "src": "(.*)", "dest": "server.js" }],
  "functions": {
    "api/*.js": {
      "maxDuration": 20,
      "memory": 5120,
      "maxBodySize": "1500mb"
    }
  }
}
